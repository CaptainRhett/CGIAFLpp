# Check that mctp processes are running
#check process protocol_handler with pidfile /var/run/protocol_handler.pid
check process protocol_handler matching "/usr/bin/protocol_handler"
    start program = "/etc/monit/scripts/protocol_handler-init start"
    stop program = "/etc/monit/scripts/protocol_handler-init stop"
    depends on mctp_pcie_bo
