# Check that rngd process is running
check process jtag_socket_svr with pidfile /var/run/jtag_socket_svr.pid
    depends on ipmid
    start program = "/etc/monit/scripts/jtagsvr-init start"
    stop program = "/etc/monit/scripts/jtagsvr-init stop"
