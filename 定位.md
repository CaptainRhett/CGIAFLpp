我使用ld_preload对malloc进行hook：
void *malloc(size_t size) {

  void *rtv = __builtin_return_address(0);

  printf("__builtin_return_address :%14p: malloc(%zu)\n", rtv, size);
  void *r = __libqasan_malloc(size);
  QASAN_DEBUG("\t\t = %p\n", r);

  return r;

}
我用qemu运行以下程序时，可以获得返回地址：
// hello_cgi.c
#include <stdio.h>
#include <stdlib.h>

void vul_func() {
    char* a = malloc(64);
    char* b = malloc(64);
    if (!a) {
        perror("malloc failed");
        return 1;
    }
    else{
        printf("malloc executed.\n");
    }
    char *query = getenv("PATH_INFO");
    if (query != NULL && *query != '\0') {
        printf("<h1>Received query: %s</h1>\n", query);
        strcpy(a, query);
    } else {
        printf("<h1>Hello from CGI!</h1>\n");
    }
    free(a);
    free(b);
    
}
void check() {
    printf("calling check().\n");
    vul_func() ;
}
int main(void) {
    printf("calling main().\n");
    check();
    
    return 0;
}
输出如下：
__builtin_return_address :       0x104f9: malloc(64)
__builtin_return_address :       0x10503: malloc(64)
与其反汇编码可以对的上：
00104ec <vul_func>:
   104ec:	b580      	push	{r7, lr}
   104ee:	b084      	sub	sp, #16
   104f0:	af00      	add	r7, sp, #0
   104f2:	2040      	movs	r0, #64	; 0x40
   104f4:	f7ff ef72 	blx	103dc <_init+0x68>
   104f8:	4603      	mov	r3, r0
   104fa:	60fb      	str	r3, [r7, #12]
   104fc:	2040      	movs	r0, #64	; 0x40
   104fe:	f7ff ef6e 	blx	103dc <_init+0x68>
   10502:	4603      	mov	r3, r0
   10504:	60bb      	str	r3, [r7, #8]
但是我如果运行一个真实的CGI程序，会大量输出如下内容：
__builtin_return_address :    0x3f912b98: malloc(996)
__builtin_return_address :    0x3f920514: malloc(4)
__builtin_return_address :    0x3f920524: malloc(4)
__builtin_return_address :    0x3f920538: malloc(12)
__builtin_return_address :    0x3f92054c: malloc(12)
__builtin_return_address :    0x3f920588: malloc(0)
__builtin_return_address :    0x3f912a90: malloc(4)
__builtin_return_address :    0x3f913f1c: malloc(4)
__builtin_return_address :    0x3f913f1c: malloc(4)
__builtin_return_address :    0x3f912a90: malloc(4)
__builtin_return_address :    0x3ffac674: malloc(448)
真实程序中调用栈更深、更复杂：
可能 hook 到了：
    glibc 的 getenv() 内部为了处理环境变量分配内存
    HTTP 解析库为了构造请求体调用了 malloc
    这时 __builtin_return_address(0) 指向的其实是 系统库中正在调用 malloc 的指令地址，
